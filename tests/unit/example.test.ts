import { describe, it, expect } from 'vitest';

describe('Example Unit Tests', () => {
  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });

  it('should handle string concatenation', () => {
    expect('Math' + ' ' + 'Pentathlon').toBe('Math Pentathlon');
  });
});
